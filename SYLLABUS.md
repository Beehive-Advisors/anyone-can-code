# Anyone Can Code — Course Syllabus

| Section | ID | Lecture Name | Format | Status |
|---------|----|----|--------|--------|
| Introduction | 0.1 | Welcome to the Course and Course Roadmap | Demo / Animation |  |
| Introduction | 0.2 | MacOS Setup | Demo / Animation |  |
| Introduction | 0.3 | Windows Setup | Demo / Animation |  |
| Introduction | 0.4 | AI Use in This Course: Since this is a fundamentals course and nearly everything we do will be in VS Code and executed in terminal, we do not want you using a coding agent (like Claude Code) but would be OK with copy/pasting from web UIs like Claude, ChatGPT, Copilot, and Gemini. | Demo / Animation |  |
| Computer Fundamentals | 1.1 | Binary, Bits, and Representation: how computers encode numbers (binary, hex), text (ASCII, Unicode), images, audio, and video as ones and zeros — everything is bits, and the only question is what you agree they mean | Demo / Animation |  |
| Computer Fundamentals | 1.2 | Logic Gates and Circuits: AND, OR, NOT, XOR — how transistors combine into gates, gates combine into adders, and adders combine into an ALU that can do math. The bridge from physics to computation. | Demo / Animation |  |
| Computer Fundamentals | 1.3 | The CPU: Fetch, Decode, Execute: what actually happens when a processor runs an instruction — clock cycles, instruction sets (x86, ARM), registers, the program counter, and how a chip executes billions of operations per second by doing very simple things very fast | Demo / Animation |  |
| Computer Fundamentals | 1.4 | Memory Hierarchy: registers, L1/L2/L3 cache, RAM, SSD, HDD — why each layer exists, the speed-cost-capacity tradeoff, locality of reference, and why cache misses matter more than most developers realize | Demo / Animation |  |
| Computer Fundamentals | 1.5 | Lab — Seeing the Machine: using lscpu to read their processor's architecture, core count, and cache sizes; lsmem or free -h to see memory; lsblk to see disk layout; and xxd to open a file and see raw hex bytes — then using Python to convert between binary, hex, decimal, and ASCII by hand | Lab | Draft |
| Computer Fundamentals | 1.6 | How a Computer is Built: Motherboards, RAM, NVMe / SSD / GPUs, Coolers / Fan Systems, PSUs, etc. | Demo / Animation |  |
| Computer Fundamentals | 1.7 | How a Computer Boots: power button → BIOS/UEFI → POST → bootloader (GRUB) → kernel loading → init/systemd → login prompt. The full sequence, demystified. Sets up the transition into Section 2. | Demo / Animation |  |
| Computer Fundamentals | 1.8 | Lab — Booting a new OS onto a Computer: Ubuntu Desktop | Lab |  |
| Computer Fundamentals | 1.9 | Remote Management and Network Booting: IPMI and BMC — how you control a machine you can't physically touch (power on, reboot, access console remotely), why this matters for data centers and bare metal servers. PXE boot — how a machine with no OS can boot from the network, the DHCP/TFTP handshake that makes it work, and why this is how thousands of servers get provisioned at scale. | Demo / Animation |  |
| Operating Systems | 2.1 | What Is an OS and Why Do We Need One: the problem of managing hardware, abstraction, and resource sharing | Demo / Animation |  |
| Operating Systems | 2.2 | Processes, Threads, and CPU Scheduling: what a process is, how the OS creates and manages them, concurrency vs. parallelism, how the OS decides which process runs next, context switching, time slicing | Demo / Animation |  |
| Operating Systems | 2.3 | Lab — Processes in Practice: ps, top, htop, kill, background/foreground jobs, &, ctrl-z, fg, bg — seeing everything from 2.2 play out in a real shell | Lab |  |
| Operating Systems | 2.4 | Virtual Memory and Paging: why every process thinks it has the whole machine, page tables, swap | Demo / Animation |  |
| Operating Systems | 2.5 | File Systems: how data is organized on disk, inodes, directories, journaling, ext4 vs. others | Demo / Animation |  |
| Operating Systems | 2.6 | The Kernel and System Calls: userspace vs. kernel space, how programs request OS services, the syscall interface | Demo / Animation |  |
| Operating Systems | 2.7 | Permissions and Ownership: the Unix permission model (user/group/other, rwx), sudo and root, and why this matters for everything we'll do later | Demo / Animation |  |
| Operating Systems | 2.8 | Lab — Navigating the File System: ls, cd, pwd, mkdir, cp, mv, rm, find, tree, absolute vs. relative paths, the root directory, home directories, and how macOS and WSL mount things differently | Lab |  |
| Operating Systems | 2.9 | Lab — Permissions and Ownership: chmod, chown, groups, ls -la, the permission model (user/group/other, rwx), sudo and root, and why this matters for everything we'll do later | Lab |  |
| Operating Systems | 2.10 | Lab — The Shell as a Programming Environment: pipes (|), redirection (>, >>, <), grep, awk, sed, wc, sort, uniq, chaining commands, environment variables, $PATH, and .bashrc/.zshrc — building the muscle memory to think in composable tools | Lab |  |
| Operating Systems | 2.11 | Lab — Installing and Managing Software: package managers (apt on Ubuntu/WSL, brew on macOS), which, man pages, curl, wget, and SSH basics — the toolkit you'll rely on for the rest of the course | Lab |  |
| Networking | 3.1 | Physical Media and Signals: copper, fiber, wireless — how bits actually travel between machines | Demo / Animation |  |
| Networking | 3.2 | The Layered Model: why we have layers, OSI vs. TCP/IP, encapsulation and decapsulation | Demo / Animation |  |
| Networking | 3.3 | Lab — Exploring Your Network Interface: ifconfig/ip addr, networkctl, identifying your interfaces (eth0, wlan0, lo), MAC addresses, and seeing the link layer on your own machine. On WSL, understanding how the virtual adapter relates to the Windows host. | Lab |  |
| Networking | 3.4 | Data Link Layer: Ethernet, MAC addresses, switches, frames, and how devices talk on a local network | Demo / Animation |  |
| Networking | 3.5 | IP and Routing: IPv4, IPv6, subnets, how routers forward packets hop by hop | Demo / Animation |  |
| Networking | 3.6 | Lab — IP and Routing in Practice: ping, traceroute/tracepath, ip route, reading a routing table, curl ifconfig.me to find your public IP, and understanding NAT by comparing your private and public addresses | Lab |  |
| Networking | 3.7 | TCP and UDP: reliable vs. unreliable transport, the three-way handshake, flow control, congestion control | Demo / Animation |  |
| Networking | 3.8 | Ports, Sockets, and Connections: how multiple applications share a single network interface | Demo / Animation |  |
| Networking | 3.9 | Lab — Watching Traffic in Real Time: netstat/ss to see open connections and listening ports, tcpdump to capture packets, reading a packet capture, and optionally Wireshark for a GUI view. Students capture a TCP handshake and identify SYN, SYN-ACK, ACK with their own eyes. | Lab |  |
| Networking | 4.10 | DNS: From Domain Name to IP: recursive resolution, root servers, caching, TTL, and why DNS is the phonebook of the internet | Demo / Animation |  |
| Networking | 4.11 | Lab — Digging into DNS: dig, nslookup, host, tracing a full recursive resolution with dig +trace, reading DNS record types (A, AAAA, CNAME, MX, TXT), checking TTLs, flushing your local DNS cache, and editing /etc/hosts to see name resolution override in action | Lab |  |
| Networking | 4.12 | HTTP and the Request-Response Model: methods, headers, status codes, cookies, and how the web actually works at the protocol level | Demo / Animation |  |
| Networking | 4.13 | Lab — Speaking HTTP by Hand: using curl -v to make requests and read raw headers, curl -X POST with payloads, inspecting response codes and cookies, and then using nc (netcat) to manually type an HTTP request to a server — demystifying that HTTP is just structured text over a TCP connection | Lab | Draft |
| Networking | 4.14 | Lab — Building a Simple Server: writing a tiny TCP server in Python (using the socket module) that listens on a port, accepts a connection, reads an HTTP request, and sends back a response. Students see every layer they've learned — sockets, ports, TCP, HTTP — come together in ~30 lines of code. | Lab |  |
| Security | 5.1 | Welcome to Security: overview of the section | Demo / Animation |  |
| Security | 5.2 | Threat Modeling and Security Thinking: how to reason about what you're protecting, from whom, and at what cost — assets, adversaries, attack surfaces, and the principle of least privilege as a design philosophy | Demo / Animation |  |
| Security | 5.3 | Cryptographic Primitives: symmetric encryption (AES), asymmetric encryption (RSA, elliptic curves), hashing (SHA-256), and MACs — what each does, when to use it, and why you never roll your own crypto | Demo / Animation |  |
| Security | 5.4 | Lab — Inspecting TLS in the Wild: using openssl s_client -connect to perform a TLS handshake manually, reading the certificate chain, checking expiration dates and SANs, comparing cipher suites across different sites, and using curl -v to see the handshake in a normal request. Optionally generating a self-signed certificate with openssl and seeing what happens when your browser encounters it. | Lab |  |
| Security | 5.5 | Authentication and Authorization: passwords, hashing and salting, session cookies, tokens, OAuth 2.0 flows, JWTs — the difference between proving who you are and determining what you're allowed to do | Demo / Animation |  |
| Security | 5.6 | Lab — Auth Mechanics: using Python to hash a password with bcrypt and verify it, decoding a JWT on the command line (echo <token> | base64 -d), inspecting a real JWT's header/payload/signature, and walking through an OAuth authorization code flow using curl against a local server to see redirects, authorization codes, and token exchange happen in plain HTTP | Lab |  |
| Security | 5.7 | Common Vulnerabilities: SQL injection, XSS, CSRF, SSRF — how each attack works mechanically at the HTTP and application layer, not just the name but the actual request that exploits it, and the specific defense that stops it | Demo / Animation |  |
| Security | 5.8 | Lab — Breaking and Fixing: using a deliberately vulnerable local app (like DVWA or Juice Shop, installed via Docker) to actually perform a SQL injection, steal a cookie via XSS, and forge a CSRF request. Then students patch each vulnerability themselves and verify the fix. Nothing teaches defense like seeing the attack work first. | Lab |  |
| Security | 5.9 | Network Security: firewalls and iptables rules, VPNs and tunneling, zero-trust architecture, and the conceptual shift from "guard the perimeter" to "verify every request" | Demo / Animation |  |
| Security | 5.10 | Lab — Firewalls and Tunnels: writing basic iptables/ufw rules to block and allow traffic, verifying with curl and nmap that the rules work, setting up an SSH tunnel (ssh -L) to access a remote service through an encrypted channel, and understanding what a VPN is actually doing under the hood by comparing it to what they just built | Lab |  |
| Security | 5.11 | Secrets Management and Supply Chain Security: why hardcoded credentials kill, environment variables vs. vaults (HashiCorp Vault, cloud KMS), dependency pinning, SBOMs, and how attacks like SolarWinds and the xz backdoor exploited the supply chain | Demo / Animation |  |
| Security | 5.12 | Lab — Secrets Hygiene: using git log and grep to find accidentally committed secrets in a repo, setting up .gitignore and git-secrets to prevent it, storing and retrieving a secret from a local Vault instance (run via Docker), and scanning a project's dependencies for known vulnerabilities with npm audit or pip audit | Lab |  |
| Application & Compute Infrastructure | 6.1 | Web Servers, App Servers, and Reverse Proxies: what Nginx, Apache, and application runtimes actually do, how they chain together, and why you almost never expose your app directly to the internet | Demo / Animation |  |
| Application & Compute Infrastructure | 6.2 | Lab — Setting Up Nginx: installing Nginx, serving a static HTML file, reading the access log, then configuring it as a reverse proxy in front of the Python server students built in Section 3. Students see the request flow through two processes and understand why that separation exists. | Lab |  |
| Application & Compute Infrastructure | 6.3 | Lab — Load Balancing with Nginx: spinning up two copies of their Python server on different ports, configuring Nginx as a load balancer with upstream, hitting it with curl in a loop and watching requests distribute, then killing one backend and observing how health checks and failover behave in real time | Lab |  |
| Application & Compute Infrastructure | 6.4 | Containers from First Principles: what problem containers solve, Linux namespaces (PID, network, mount), cgroups for resource limits, layered file systems — what Docker is actually doing under the hood vs. the magic it hides | Demo / Animation |  |
| Application & Compute Infrastructure | 6.5 | Lab — Containers by Hand, Then with Docker: first, using unshare and chroot to manually create a primitive container so students see that containers are just Linux kernel features. Then switching to Docker: writing a Dockerfile, building an image, running a container, docker exec to poke around inside, and docker ps / docker logs to manage it. | Lab |  |
| Application & Compute Infrastructure | 6.6 | Orchestration and Kubernetes Concepts: why you need orchestration once you have more than a few containers — pods, services, deployments, replicas, scheduling, the control loop, and the declarative model | Demo / Animation |  |
| Application & Compute Infrastructure | 6.7 | Lab — Kubernetes Basics: using minikube or kind to run a local cluster, deploying their containerized Python app with a Deployment manifest, exposing it with a Service, scaling it up with kubectl scale, and watching Kubernetes restart a crashed pod automatically | Lab |  |
| Application & Compute Infrastructure | 6.8 | Relational Databases from the Inside: how data lives on disk, B-trees and pages, what an index actually is, how the query planner decides the fastest path, and the write-ahead log | Demo / Animation |  |
| Application & Compute Infrastructure | 6.9 | Transactions and ACID: what atomicity, consistency, isolation, and durability mean concretely, isolation levels, locking, and why "just use a transaction" is more nuanced than it sounds | Demo / Animation |  |
| Application & Compute Infrastructure | 6.10 | Lab — Postgres Deep Dive: installing Postgres (or running it in Docker), creating tables, writing queries, then using EXPLAIN ANALYZE to see how the planner executes them. Adding an index and watching the plan change. Opening two psql sessions to create a dirty read and a deadlock on purpose. | Lab |  |
| Application & Compute Infrastructure | 6.11 | Non-Relational Databases: key-value (Redis), document (MongoDB), columnar (Cassandra), graph (Neo4j) — what problem each data model solves well, what it sacrifices, and how to pick the right one | Demo / Animation |  |
| Application & Compute Infrastructure | 6.12 | Lab — Redis and Mongo Side by Side: running Redis and MongoDB in Docker, storing and retrieving data in each, and doing a small exercise that highlights the tradeoff — something natural in a document model but awkward in key-value, and something where Redis's speed and simplicity wins | Lab |  |
| Application & Compute Infrastructure | 6.13 | Replication, Sharding, and Distribution: how databases scale beyond one machine — leader-follower replication, replication lag, horizontal sharding, consistent hashing, the CAP theorem as a practical tradeoff | Demo / Animation |  |
| Application & Compute Infrastructure | 6.14 | Message Queues and Async Processing: why synchronous request-response breaks down, decoupling producers and consumers, backpressure, delivery guarantees, dead letter queues, and event-driven architecture | Demo / Animation |  |
| Application & Compute Infrastructure | 6.15 | Lab — Pub/Sub with Redis and a Task Queue: using Redis pub/sub to send messages between two terminal sessions, then building a simple task queue — one Python script pushes jobs, another pulls and processes them. Students simulate a slow consumer, watch the queue back up, and implement a basic retry mechanism. | Lab |  |
| The Application Layer | 7.1 | The Big Picture: how a modern web application is structured — client, server, database, and the request cycle that connects them. Why the ecosystem split into frontend and backend in the first place. | Demo / Animation |  |
| The Application Layer | 7.2 | Frontend Fundamentals: HTML as structure, CSS as presentation, JavaScript as behavior — what the browser actually does when it parses and renders a page, the DOM, and why this trio is the foundation everything else compiles down to | Demo / Animation |  |
| The Application Layer | 7.3 | Frontend Frameworks and the Component Model: what React, Vue, and Svelte actually do — components as the unit of UI, state management, reactivity, the virtual DOM vs. fine-grained reactivity | Demo / Animation |  |
| The Application Layer | 7.4 | Meta-Frameworks and the Rendering Spectrum: Next.js, Nuxt, SvelteKit — what they add on top of the base framework, the tradeoffs between SPAs, server-side rendering, static generation, and hybrid approaches | Demo / Animation |  |
| The Application Layer | 7.5 | Backend Runtimes and Frameworks: Node.js and the event loop, Python's WSGI/ASGI model, Go's goroutines — different concurrency models for different workloads. Express, FastAPI, Flask, Rails, and when each philosophy fits | Demo / Animation |  |
| The Application Layer | 7.6 | Serverless and Functions-as-a-Service: what "no servers" really means, cold starts, execution limits, statelessness as a constraint, event triggers, and when serverless is the right tool vs. when it becomes a painful trap | Demo / Animation |  |
| The Application Layer | 7.7 | Lab — Building an API: writing a REST API in both Express and FastAPI that serves the same endpoints — CRUD operations against an in-memory data store. Students see how two very different languages and frameworks solve the same problem. | Lab |  |
| The Application Layer | 7.8 | How Frontend Talks to Backend: the fetch/XHR cycle, JSON as the lingua franca, CORS and why it exists, environment variables for API URLs | Demo / Animation |  |
| The Application Layer | 7.9 | Lab — Full-Stack Wiring: connecting the React frontend to the Express or FastAPI backend, backed by Postgres. Students build a simple app where they create a record in the UI, watch it hit the API, see it written to the database, and query it back. | Lab |  |
| The Application Layer | 7.10 | Lab — Three APIs, One Service: building the same simple service (a to-do list) exposed three ways — REST with curl, GraphQL where students write queries that fetch exactly the fields they want, and gRPC with a .proto file and generated client | Lab |  |
| The Application Layer | 7.11 | The Build Pipeline: what Vite, Webpack, and esbuild actually do — bundling, minification, tree-shaking, code splitting, transpilation. Why your source code isn't what ships to the browser. | Demo / Animation |  |
| The Application Layer | 7.12 | Package Managers and Dependency Ecosystems: npm, pip, cargo — how packages are resolved, lockfiles, semantic versioning, the node_modules problem, virtual environments in Python, and why dependency management is both essential and a source of endless pain | Demo / Animation |  |
| The Application Layer | 7.13 | Choosing a Stack: there is no "best" stack — how to think about the decision based on team expertise, ecosystem maturity, performance requirements, hiring, and the problem you're actually solving | Demo / Animation |  |
| Cloud Architecture Patterns | 8.1 | How Cloud Providers Actually Work: multi-tenancy, hypervisors (KVM, Xen), software-defined networking, the control plane vs. data plane, and what you're really renting when you spin up an instance | Demo / Animation |  |
| Cloud Architecture Patterns | 8.2 | Core Cloud Services Demystified: compute (EC2, Cloud Run), storage (S3, block vs. object vs. file), and networking primitives (VPCs, subnets, security groups, elastic IPs) | Demo / Animation |  |
| Cloud Architecture Patterns | 8.3 | Lab — Building a VPC from Scratch: using a free-tier cloud account (AWS or GCP) to manually create a VPC, public and private subnets, an internet gateway, route tables, and security groups. Then launching an instance, SSHing in, and running their containerized app from Section 6. | Lab |  |
| Cloud Architecture Patterns | 8.4 | IAM and Cloud Security: identity and access management, policies, roles, the principle of least privilege applied to cloud resources, service accounts, and why misconfigured IAM is the number one cause of cloud breaches | Demo / Animation |  |
| Cloud Architecture Patterns | 8.5 | Lab — IAM the Hard Way: creating an IAM user with no permissions, trying to access an S3 bucket and getting denied, then incrementally attaching policies until it works. Students experience the permission model as a series of "no" until you explicitly say "yes." | Lab |  |
| Cloud Architecture Patterns | 8.6 | Serverless and Functions-as-a-Service (Cloud): cold starts, execution limits, statelessness as a constraint, event triggers in the cloud context | Demo / Animation |  |
| Cloud Architecture Patterns | 8.7 | Lab — Deploying a Lambda Function: writing a small function that processes an event (e.g., an image upload to S3 triggers thumbnail generation, or an HTTP request returns transformed data), deploying it, hitting it with curl, checking the logs in CloudWatch | Lab |  |
| Cloud Architecture Patterns | 8.8 | Microservices vs. Monoliths: the architectural spectrum rather than a binary choice, service boundaries and domain-driven design, inter-service communication, the hidden costs of distribution, and when a monolith is the smarter choice | Demo / Animation |  |
| Cloud Architecture Patterns | 8.9 | API Design — REST, GraphQL, gRPC: what each protocol optimizes for, request/response shapes, schema design and contracts, versioning strategies | Demo / Animation |  |
| Cloud Architecture Patterns | 8.10 | Lab — Three APIs, One Service (Cloud): building the same simple service exposed three ways in a cloud-deployed context | Lab |  |
| Cloud Architecture Patterns | 8.11 | Managed Services and Build vs. Buy: when to use a managed database (RDS, Cloud SQL) vs. running your own, managed Kubernetes (EKS, GKE) vs. self-hosted — the decision framework around operational burden, cost, and vendor lock-in | Demo / Animation |  |
| Cloud Architecture Patterns | 8.12 | Edge Computing and the Modern Stack: pushing compute closer to users, edge functions (Cloudflare Workers, Lambda@Edge), the convergence of CDN and compute, and the constraints of running code in 200+ locations simultaneously | Demo / Animation |  |
| Cloud Architecture Patterns | 8.13 | Lab — Deploying to the Edge: deploying a small edge function on Cloudflare Workers (free tier), adding logic that returns different responses based on geographic headers, and measuring the latency difference between an edge response and a round trip to origin | Lab |  |
| Cloud Architecture Patterns | 8.14 | Full Request Lifecycle — The Capstone Trace: following a single production request through every layer of the course — browser → DNS → BGP routing → CDN edge → TLS handshake → load balancer → Kubernetes pod → application code → database query → response. Every concept from the entire course shows up in this one story. | Demo / Animation |  |
| GPU Compute Infrastructure for LLMs | 9.1 | Why GPUs Exist: how GPUs differ from CPUs architecturally — SIMD, thousands of cores optimized for throughput over latency, and why matrix multiplication is the operation that changed everything | Demo / Animation |  |
| GPU Compute Infrastructure for LLMs | 9.2 | GPU Memory and Bandwidth: HBM vs. GDDR, memory bandwidth as the key bottleneck, arithmetic intensity, and the roofline model for understanding whether a workload is compute-bound or memory-bound | Demo / Animation |  |
| GPU Compute Infrastructure for LLMs | 9.3 | The NVIDIA Ecosystem: CUDA as the programming model, cuDNN, NVLink for multi-GPU communication, NVSwitch, DGX systems, and why NVIDIA's moat is software as much as hardware | Demo / Animation |  |
| GPU Compute Infrastructure for LLMs | 9.4 | Lab — Getting Oriented with GPUs: running nvidia-smi, reading utilization and memory, running a matrix multiplication in PyTorch on CPU vs. GPU on a cloud instance, and measuring the speedup firsthand | Lab |  |
| GPU Compute Infrastructure for LLMs | 9.5 | The Transformer as an Infrastructure Problem: why attention is O(n²) in sequence length, why model size maps to memory, what "7B parameters" means in gigabytes, and framing the architecture as a compute and memory problem rather than a math lecture | Demo / Animation |  |
| GPU Compute Infrastructure for LLMs | 9.6 | Inference Infrastructure: how serving differs from training — latency matters now, batching strategies, the KV cache, prefill vs. decode phases, continuous batching, and the throughput-latency tradeoff | Demo / Animation |  |
| GPU Compute Infrastructure for LLMs | 9.7 | Inference Optimization: quantization (INT8, INT4, GPTQ, AWQ), speculative decoding, PagedAttention and vLLM, TensorRT-LLM, torch.compile — each technique mapped to the specific bottleneck it solves | Demo / Animation |  |
| GPU Compute Infrastructure for LLMs | 9.8 | Lab — Running Inference with Docker: pulling and running vLLM and Ollama as Docker containers, loading a model in each, hitting them with curl, comparing the developer experience and use cases, and monitoring GPU memory and utilization with nvidia-smi | Lab |  |
| GPU Compute Infrastructure for LLMs | 9.9 | Lab — Benchmarking and Quantization Tradeoffs: using the vLLM container to benchmark tokens per second, time to first token, and concurrent request throughput. Then loading an INT4 quantized version of the same model, rerunning the benchmarks, and comparing speed, memory footprint, and output quality side by side | Lab |  |
| GPU Compute Infrastructure for LLMs | 9.10 | The GPU Cloud Landscape and Cost Optimization: on-demand vs. reserved vs. spot, hyperscalers vs. GPU-native providers (CoreWeave, Lambda Labs, Together), right-sizing, batching to maximize utilization, auto-scaling, and total cost of ownership | Demo / Animation |  |
| GPU Compute Infrastructure for LLMs | 9.11 | Edge and Local Inference: running models on consumer hardware, Apple Silicon and unified memory, GGUF and llama.cpp, Ollama for local development, quantization-quality tradeoffs on personal machines | Demo / Animation |  |
| GPU Compute Infrastructure for LLMs | 9.12 | Lab — Local Inference: downloading a quantized model and running it locally with llama.cpp or Ollama, measuring tokens per second on their own machine, comparing INT4 vs. INT8 quality and speed | Lab |  |
